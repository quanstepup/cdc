{
  "name": "jdbc-sink",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "1",
    "topics.regex": "sink-db.public.(.*)",
    "connection.url": "jdbc:postgresql://postgres-target:5432/inventory?user=postgres&password=123",
    "dialect.name": "PostgreSqlDatabaseDialect",
    "table.name.format": "${topic}",
    "auto.create": "true",
    "insert.mode": "upsert",
    "delete.enabled": "true",
    "pk.fields": "id",
    "pk.mode": "record_key",
    "key.converter":"org.apache.kafka.connect.json.JsonConverter",
    "key.converter.schemas.enable":"true",
    "value.converter":"org.apache.kafka.connect.json.JsonConverter",
    "value.converter.schemas.enable":"true",
    "transforms.route.type": "org.apache.kafka.connect.transforms.RegexRouter",
    "transforms.route.regex": "([^.]+)\\.([^.]+)\\.([^.]+)",
    "transforms.route.replacement": "$3",
    "mode":"incrementing",
    "incrementing.column.name":"id",
    "transforms": "unwrap, route",
    "transforms.unwrap.type": "io.debezium.transforms.ExtractNewRecordState",
    "transforms.unwrap.drop.tombstones": "false",
    "delete.handling.mode": "none"
  }
}

//"transforms.copyFieldToKey.type":"org.apache.kafka.connect.transforms.ValueToKey",
//"transforms.copyFieldToKey.fields":"id",
//"transforms.extractKeyFromStruct.type":"org.apache.kafka.connect.transforms.ExtractField$Key",
//"transforms.extractKeyFromStruct.field":"id",

//"transforms": "unwrap, route, copyFieldToKey, extractKeyFromStruct",
